<odoo>

	<!--
        <record id="auth_signup.set_password_email" model="mail.template" />

		NOTE: this template is marked as `noupdate` in DB
        hence we are not able to update it via xml.
        We moved customization to `odoo.songs.install.post:change_signup_email`.
	-->


</odoo>
