<?xml version="1.0" encoding="utf-8"?>
<odoo>

<template id="assets_frontend" inherit_id="website.assets_frontend" name="Proposal Assets">
  <xpath expr="." position="inside">
    <script type="text/javascript" src="/specific_project_proposal/static/src/js/project_proposal.js"/>
  </xpath>
</template>

<template id="account" name="Account" inherit_id="website_portal.account">
  <xpath expr="//div[contains(@class,'o_my_documents')]" position="inside">
    <t t-call="specific_project_proposal.matches_overview" />
    <t t-call="specific_project_proposal.proposals_overview" />
  </xpath>
</template>

<template id="matches_overview" name="Proposals">
    <div id="matches_overview" class="col-md-6">
        <h3 class="page-header">Matches</h3>
        <t t-if="not matches">
            <p>No matches</p>
            <p>Please, verify you have set your expertises and industry on your profile.</p>
        </t>
        <t t-if="matches">
            <table class="table table-hover o_my_status_table">
            <thead>
            </thead>
            <t t-foreach="matches" t-as="proposal">
                <tr t-att-class="'hidden to_hide' if proposal_index &gt; 3 else ''">
                <td>
                    <a t-attf-href="/proposals/detail/#{ slug(proposal) }?filters=match">
                    <t t-if="proposal.is_new">
                        &lt;b&gt;NEW!
                    </t>
                    <span t-field="proposal.name"/>
                    <t t-if="proposal.is_new">
                        &lt;/b&gt;
                    </t>
                    </a>
                </td>
                <td>
                    <span t-if="not proposal.website_published" class="label label-danger">not published</span>
                </td>
                <td>
                    <span t-field="proposal.create_date" class="text-muted" t-field-options='{"format": "d MMMM y"}'/>
                </td>
                <td>
                    <a t-attf-href="/proposals/detail/#{ slug(proposal) }?filters=match" class="text-muted">
                    Read More
                    </a>
                </td>
                </tr>
            </t>
            <tr t-att-class="'hidden' if len(matches) &lt; 4 else ''">
                <td colspan="4"><a href="" class="o_my_show_more btn btn-xs btn-default"><span>Show more</span><span class="hidden">Show less</span></a></td>
            </tr>
            </table>
            <a class="btn btn-primary btn-show-proposal center-block" t-attf-href="/market" title="Market">
                Market
            </a>
        </t>
    </div>
</template>

<template id="proposals_overview" name="Proposals">
    <div id="proposals_overview" class="col-md-6">
        <h3 class="page-header">Proposals</h3>
        <t t-if="not proposals">
            <p>You have currently registred no proposals on your account.</p>
        </t>
        <t t-if="proposals">
            <table class="table table-hover o_my_status_table">
            <thead>
            </thead>
            <t t-foreach="proposals" t-as="proposal">
                <tr t-att-class="'hidden to_hide' if proposal_index &gt; 3 else ''">
                <td>
                    <a t-attf-href="/my/proposals/edit/#{ slug(proposal) }">
                    <b t-field="proposal.name"/>
                    </a>
                </td>
                <td>
                    <span t-if="not proposal.website_published" class="label label-danger">not published</span>
                </td>
                <td>
                    <span t-field="proposal.create_date" class="text-muted" t-field-options='{"format": "d MMMM y"}'/>
                </td>
                <td>
                    <a t-attf-href="/my/proposals/edit/#{ slug(proposal) }" class="text-muted">
                    Edit
                    </a>
                </td>
                </tr>
            </t>
            <tr t-att-class="'hidden' if len(proposals) &lt; 4 else ''">
                <td colspan="4"><a href="" class="o_my_show_more btn btn-xs btn-default"><span>Show more</span><span class="hidden">Show less</span></a></td>
            </tr>
            </table>
        </t>
        <a class="btn btn-primary btn-show-proposal" t-attf-href="/my/proposals" title="Show all proposals">
            Show all proposals
        </a>
        <a class="btn btn-primary btn-add-proposal" t-attf-href="/my/proposals/add" title="Add a proposal">
            Add a proposal
        </a>
    </div>
</template>

<template id="proposal_index" name="Proposals">
  <t t-call="website.layout">
    <div id="wrap">
      <div class="oe_structure">
        <section class="content members-list col-md-12 opt_dark_bg">
          <div class="container">
           <div class="row content-wrap">
                <div class="content-icon">
                    <img src="/specific_project_proposal/static/src/img/market.png"></img>
                </div>
              <div class="col-md-4 col-md-offset-4 text-center mb16">
                <h2>
                  <t t-if="my">
                    My Project proposals
                  </t>
                  <t t-if="not my">
                    Market
                  </t>
                </h2>
              </div>
            </div>
          </div>
          <div class="col-md-4 col-md-offset-4">
          <form class="navbar-form" role="search" t-attf-action="#{url}" method="get">
            <div class="input-group">
              <div class="oe_search">
                <input type="text" name="search_name" class="search-query form-control oe_search_box" placeholder="Search project..." t-att-value="search_name" />
              </div>
              <span class="input-group-btn">
                <button type="submit" class="btn btn-default oe_search_button"><i class="fa fa-search"/> Search</button>
              </span>
            </div>
            <div>
              <div class="oe_search">
                <input name="search_industries" class="search-query form-control oe_search_box js_select2_categories" placeholder="Industries" t-attf-data-init-value="#{industries}" value="Industry"/>
                <input name="search_expertises" class="search-query form-control oe_search_box js_select2_expertises" placeholder="Expertises" t-attf-data-init-value="#{expertises}" value="Expertise"/>
              </div>
            </div>
            <div>
              <div class="oe_search">
                <div class="styled-select">
                  <select name="search_country" class="form-control">
                    <option value="">Country...</option>
                    <t t-foreach="countries or []" t-as="country">
                      <option t-att-value="country.id"
                        t-att-selected="country.id == selected_country_id">
                        <t t-esc="country.name" />
                      </option>
                    </t>
                  </select>
                </div>
                <input type="text" name="search_location" class="search-query form-control oe_search_box" placeholder="Location" t-att-value="search_location" />
              </div>
            </div>
          </form>
          </div>
        </section>
      </div>

      <div class="container oe_website_proposals">
        <div class="row">
          <div class="hidden" id="proposals_grid_left">

          </div>
          <div class="col-md-12" id="proposals_grid">
            <div t-if="not proposals">
              <div class="text-center text-muted">
                <h4 class="css_editable_display">No project proposal found</h4>
                <t t-if="not my">
                  <h4 class="css_editable_hidden">There isn't project proposal published now, click <a t-attf-href="/page/website.contactus">here</a> to contact us</h4>
                </t>
                <t t-if="my">
                  <h4 class="css_editable_hidden">Go in <a href="/my">your account</a> to create your proposals.</h4>
                </t>
              </div>
            </div>
            <div class="col-md-4 col-md-offset-4">
            <ul class="media-list" t-if="proposals">
              <li t-foreach="proposals" t-as="proposal" class="media">
                <div class="media-body proposal_item" t-att-data-publish="proposal.website_published and 'on' or 'off'">
                  <div class="text-muted">
                    <span t-field="proposal.create_date" t-field-options='{"format": "d MMMM y"}'/>
                  </div>
                  <h3 class="media-heading">
                    <a t-att-href="detail_url.format(slug(proposal))">
                      <span t-field="proposal.name"/>
                    </a>
                  </h3>
                  <t t-if="proposal.owner_id.id != uid">
                    <a class="hide_proposal fa fa-eye-slash no-decoration pull-right"
                      t-attf-data-href="/proposals/proposal/#{ slug(proposal)}/hide"
                      title="Don't show again this project proposal"/>
                  </t>
                  <span t-if="not proposal.website_published" class="label label-danger">not published</span>
                  <p t-field="proposal.website_short_description"/>
                  <div>
                    <b>Industries:</b>
                    <t t-foreach="proposal.industry_ids" t-as="industry">
                      <span t-attf-class="label #{industry_ids and industry.id in industry_ids and 'label-primary' or 'label-default'}" t-field="industry.name"
                        style="margin-right: 4px;"/>
                    </t>
                    <a t-att-href="detail_url.format(slug(proposal))">
                      Read More <i class="fa fa-arrow-right"/>
                    </a>
                  </div>
                </div>
              </li>

            </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
    <t t-call="website.pager"/>
  </t>
</template>

<template id="layout">
  <t t-call="website.layout">
    <t t-set="additional_title">Proposal Detail</t>
    <div id="wrap" class="js_proposal o_website_proposal">
      <div class="oe_structure"/>

      <div class="container mt8">
        <div class="row">
          <div class="col-sm-7" id="proposal_menu">
            <ol class="breadcrumb mb0">
              <li>
                <a t-attf-href="#{return_link}">
                  <t t-if="filters == 'my'">
                    My proposals
                  </t>
                  <t t-if="filters == 'match'">
                    My account
                  </t>
                  <t t-if="filters not in ['my', 'match']">
                    Proposals
                  </t>
                </a>
              </li>
              <li class="active"><span t-field="proposal.name"></span></li>
            </ol>
          </div>
          <t t-if="proposal.owner_id.id == uid">
            <div class="col-sm-5">
              <t t-call="website.publish_management">
                <t t-set="object" t-value="proposal"/>
                <t t-set="publish_edit" t-value="True"/>
              </t>
            </div>
          </t>
        </div>
      </div>
      <t t-raw="0"/>
      <div class="oe_structure"/>
    </div>
  </t>
</template>

<template id="proposal_detail" name="Proposal Detail">
  <t t-call="specific_project_proposal.layout">

    <div class="oe_structure" style="clear:both;">
      <section class="content members-list opt_dark_bg">
        <a class="btn btn-primary btn-show-proposal" t-attf-href="#{previous_link}" title="Previous Proposal">
            Previous
        </a>
        <a class="btn btn-primary btn-show-proposal" t-attf-href="#{next_link}" title="Next Proposal">
            Next
        </a>
      </section>
      <section class="content members-list">
        <div class="content-wrap">
          <div class="text-muted">
            <span t-field="proposal.create_date" t-field-options='{"widget": "date"}'/>
          </div>

          <address>
            <t t-set="partner" t-value="proposal.sudo().owner_id.partner_id"/>
            <t t-set="partner" t-value="partner.parent_id if partner.parent_id else partner"/>
            <t t-if="is_associate">
              <b t-field="partner.name"/>
              <div t-field="partner.self" t-field-options='{
                "widget": "contact",
                "fields": ["address", "phone", "fax", "email"]
              }'/>
            </t>
          </address>

          <h1 t-field="proposal.name"/>

          <p><b t-field="proposal.website_short_description"/></p>

          <t t-if="is_associate">
            <p t-field="proposal.website_description"/>
          </t>

          <div t-if="is_associate">
            <b>Country:</b>
            <span t-field="proposal.country_id"/>
          </div>
          <div t-if="is_associate">
            <b>Location:</b>
            <span t-field="proposal.location"/>
          </div>

          <div>
            <b>Duration:</b>
            <span t-field="proposal.start_date"/> - <span t-field="proposal.stop_date"/>
          </div>
          <div>
            <b>Industries:</b>
            <t t-foreach="proposal.industry_ids" t-as="industry">
              <span t-field="industry.name"/>
            </t>
          </div>
          <div t-if="is_associate">
            <b>Expertises:</b>
            <t t-foreach="proposal.expertise_ids" t-as="expertise">
              <span t-field="expertise.name"/>
            </t>
          </div>
        </div>
      </section>
      <t t-if="is_public_user">
        <section class="content opt_dark_bg">
          <h1>Membership</h1>
          <a class="btn btn-primary" t-att-href="'/web/presignup?redirect=' + detail_url">
            Sign Up
          </a>
          <a class="btn btn-primary" t-att-href="'/web/login?redirect=' + detail_url">
            Login
          </a>
        </section>
      </t>
      <t t-if="is_free_member">
        <section class="content opt_dark_bg">
          <h1>Membership</h1>
          <a class="btn btn-primary" t-att-href="'/my/membership?redirect=' + detail_url">
            Become an associate member
          </a>
        </section>
      </t>
      <section>
        <a class="btn btn-primary btn-show-proposal" t-attf-href="#{return_link}">
          <t t-if="filters == 'match'">
              Return to my account
          </t>
          <t t-if="filters != 'match'">
              Return to list
          </t>
        </a>
      </section>
    </div>
  </t>
</template>

<template id="proposal_edit" name="Proposal Edit">
  <t t-call="specific_project_proposal.layout">
    <div class="oe_structure oe_website_proposals" style="clear:both;">
      <form t-attf-action="/my/proposals/edit/#{ slug(proposal) }" method="post" enctype="multipart/form-data" t-attf-data-success_page="/my">
        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
        <div class="row o_website_portal_details">
          <h2>Proposal</h2>
          <div class="col-md-12">
            <div class="row">
              <div class="">
                <div t-if="error_message" class="alert alert-danger">
                  <t t-foreach="error_message" t-as="err">
                    <t t-esc="err" />
                    <br />
                  </t>
                </div>
              </div>
              <div t-attf-class="form-group row #{error.get('name') and 'has-error' or ''} ">
                <label class="form-control-label col-sm-2" for="name">Proposal name (*)
                </label>
                <input type="text" name="name" class="form-control"
                  t-att-value="name or proposal.name" />
              </div>
              <div t-attf-class="form-group row #{error.get('location') and 'has-error' or ''} ">
                <label class="form-control-label col-sm-2" for="location">Location
                </label>
                <input type="text" name="location" class="form-control"
                  t-att-value="location or proposal.location" />
              </div>
              <div
                t-attf-class="form-group row #{error.get('country_id') and 'has-error' or ''} ">
                <label class="form-control-label col-sm-2" for="country_id">Country
                </label>
                <div class="styled-select">
                  <select name="country_id" class="form-control">
                    <option value="">Country...</option>
                    <t t-foreach="countries or []" t-as="country">
                      <option t-att-value="country.id"
                        t-att-selected="country.id == (country_id or proposal.country_id.id)">
                        <t t-esc="country.name" />
                      </option>
                    </t>
                  </select>
                </div>
              </div>

              <div
                t-attf-class="form-group separate row #{error.get('website_short_description') and 'has-error' or ''} ">
                <label class="form-control-label label-optional col-sm-2" for="website_short_description">
                  Teaser text (*)
                </label>
                <textarea placeholder="200 characters left" name="website_short_description" class="form-control"><t t-esc="website_short_description or proposal.website_short_description"/></textarea>
              </div>
              <div
                t-attf-class="form-group separate row #{error.get('website_description') and 'has-error' or ''} ">
                <label class="form-control-label label-optional col-sm-2" for="website_description">
                  Description (*)
                </label>
                <textarea name="website_description" class="form-control"><t t-esc="website_description or proposal.website_description"/></textarea>
              </div>
              <div
                t-attf-class="form-group input-group date #{error.get('start_date') and 'has-error' or ''}">
                <label class="form-control-label label-optional col-sm-2"
                  for="start_date">Start date</label>
                <input type="text" class="form-control" data-date-format="YYYY-MM-DD" name="start_date" placeholder="YYYY-MM-DD"
                  t-att-value="start_date or proposal.start_date"
                  />
                <span class="input-group-addon"><span class="fa fa-calendar"></span></span>
              </div>

              <div
                t-attf-class="form-group input-group date #{error.get('stop_date') and 'has-error' or ''}">
                <label class="form-control-label label-optional col-sm-2"
                  for="stop_date">Stop date</label>
                <input type="text" class="form-control" data-date-format="YYYY-MM-DD" name="stop_date" placeholder="YYYY-MM-DD"
                  t-att-value="stop_date or proposal.stop_date"/>
                <span class="input-group-addon"><span class="fa fa-calendar"></span></span>
              </div>

              <div
                t-attf-class="form-group row #{error.get('post_industries') and 'has-error' or ''} ">
                <label class="form-control-label col-sm-2" for="industry_ids">Industry (*)
                </label>
                <input name="post_industries" placeholder="Industry" class="form-control js_select2_categories" t-attf-data-init-value="#{industries}" value="Industry" />
              </div>
              <div
                t-attf-class="form-group row #{error.get('post_expertises') and 'has-error' or ''} ">
                <label class="form-control-label col-sm-2" for="expertise_ids">Expertise (*)
                </label>
                <input name="post_expertises" placeholder="Expertise" class="form-control js_select2_expertises" t-attf-data-init-value="#{expertises}" value="Expertise" />
              </div>
            </div>
            <div class="button-wrap">
              <button type="submit" name="confirm" value="Confirm" class="btn btn-default btn-primary mb32 ">Confirm</button>
              <button type="submit" name="publish" t-att-value="'Unpublish' if proposal.website_published else 'Publish'" class="btn btn-default btn-primary mb32">
                <span t-att-class="'hidden' if proposal.website_published else ''">
                  Publish
                </span>
                <span t-att-class="'hidden' if not proposal.website_published else ''">
                  Unpublish
                </span>
              </button>
              <a class="btn btn-default btn-primary mb32 delete_proposal" t-attf-href="/proposals/proposal/#{slug(proposal)}/delete_confirm">Delete</a>
            </div>
              <a class="btn btn-default btn-primary mb32" t-attf-href="/my">Back</a>
          </div>
        </div>
      </form>
    </div>
  </t>
</template>

<template id="proposal_delete_confirm" name="Proposal Detail">
  <t t-call="specific_project_proposal.layout">

    <div class="oe_structure" style="clear:both;">
      <section class="content members-list opt_dark_bg">
        <h3>Are you sure you want to remove this proposal?</h3>
        <a class="btn btn-primary btn-show-proposal" title="Confirm"
          t-attf-href="/proposals/proposal/#{slug(proposal)}/delete">
          Confirm
        </a>
        <a class="btn btn-primary btn-show-proposal" title="Cancel"
          t-attf-href="/my/proposals/edit/#{slug(proposal)}">
          Cancel
        </a>
      </section>
    </div>
  </t>
</template>

</odoo>
