<?xml version="1.0" encoding="utf-8"?>
<odoo>

	<template id="website_portal_profile-details" name="Profile details" inherit_id="website_portal.details">
		<xpath expr="//div[@class='row o_website_portal_details']" position="replace">
                        <div class="row o_website_portal_details">
                            <div class="col-md-8">
                                <div class="row">
                                    <div class="col-md-12">
                                      <div t-if="error_message" class="alert alert-danger">
                                          <t t-foreach="error_message" t-as="err"><t t-esc="err"/><br /></t>
                                      </div>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('name') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="contact_name">Your Name</label>
                                        <input type="text" name="name" class="form-control" t-att-value="name or partner.name" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('street') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="street">Company Name</label>
                                        <input type="text" name="street" class="form-control" t-att-value="street or partner.street"/>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('street') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="street">Street</label>
                                        <input type="text" name="street2" class="form-control" t-att-value="street2 or partner.street2" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('zip') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="zipcode">Zip / Postal Code</label>
                                        <input type="text" name="zipcode" class="form-control" t-att-value="zipcode or partner.zip" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('city') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="city">City</label>
                                        <input type="text" name="city" class="form-control" t-att-value="city or partner.city" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('country_id') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="country_id">Country</label>
                                        <div class="styled-select">
	                                        <select name="country_id" class="form-control">
	                                            <option value="">Country...</option>
	                                            <t t-foreach="countries or []" t-as="country">
	                                                <option t-att-value="country.id" t-att-selected="country.id == partner.country_id.id">
	                                                    <t t-esc="country.name" />
	                                                </option>
	                                            </t>
	                                        </select>
                                        </div>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('phone') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="phone">Phone</label>
                                        <input type="tel" name="phone" class="form-control" t-att-value="phone or partner.phone" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('email') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="contact_name">Email</label>
                                        <input type="email" name="email" class="form-control" t-att-value="email or partner.email" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('website') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="website">URL</label>
                                        <input type="text" name="website" class="form-control" t-att-value="partner.website"/>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('facebook') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="facebook">Facebook</label>
                                        <input type="text" name="facebook" class="form-control" t-att-value="partner.facebook"/>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('twitter') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="twitter">Twitter</label>
                                        <input type="text" name="twitter" class="form-control" t-att-value="partner.twitter"/>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('werbeversprechen') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="werbeversprechen">Werbeversprechen</label>
                                        <input type="text" name="werbeversprechen" class="form-control" t-att-value="partner.werbeversprechen"/>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('category_id') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="category_id">Category</label>
                                        <select name="category_id" class="form-control">
                                            <option value="">Category</option>
                                            <t t-foreach="categories or []" t-as="category">
                                                <option t-att-value="category.id" t-att-selected="category.id == partner.category_id.id">
                                                    <t t-esc="category.name" />
                                                </option>
                                            </t>
                                        </select>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('area_ids') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="area_ids">Area</label>
                                        <select name="area_ids" class="form-control">
                                            <option value="">Areas</option>
                                            <t t-foreach="areas or []" t-as="area">
                                                <option t-att-value="area.id" t-att-selected="area.id == partner.area_ids.id">
                                                    <t t-esc="area.name" />
                                                </option>
                                            </t>
                                        </select>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('expertise') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="expertise">URL</label>
                                        <input type="text" name="expertise" class="form-control" t-att-value="partner.expertise"/>
                                    </div>
                                    <div class="clearfix" />
                                    <div t-if="has_check_vat" t-attf-class="form-group #{error.get('vat') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="vat">VAT Number</label>
                                        <input type="text" name="vat" class="form-control" t-att-value="vat or partner.vat" />
                                    </div>

                                    <div class="clearfix" />
                                    <div t-attf-class="form-group #{error.get('state_id') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="state_id">State / Province</label>
                                        <select name="state_id" class="form-control">
                                            <option value="">select...</option>
                                            <t t-foreach="states or []" t-as="state">
                                                <option t-att-value="state.id" style="display:none;" t-att-data-country_id="state.country_id.id" t-att-selected="state.id == partner.state_id.id">
                                                    <t t-esc="state.name" />
                                                </option>
                                            </t>
                                        </select>
                                    </div>
                                    <input type="hidden" name="redirect" t-att-value="redirect"/>
                                </div>
				<div class="clearfix" />
				<div>
					<button type="submit" class="btn btn-default btn-primary pull-left mb32 ">Confirm</button>
					<a t-attf-href="/my/projects/new" class="btn btn-primary pull-right mb32">Add project</a>
				</div>
                            </div>
                        </div>
		</xpath>
	</template>

</odoo>
