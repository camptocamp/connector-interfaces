<?xml version="1.0"?>

<odoo>

    <!-- override address format -->
    <record id="base.ch" model="res.country">
        <field name="address_format" eval="'%(street)s\n%(street)s\n%(zip)s %(city)s\n%(country_name)s'" />
    </record>

</odoo>
